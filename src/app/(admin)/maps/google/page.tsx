import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Google Maps' }

const GoogleMaps = () => {
  return <div className="p-3">Google Maps indisponível no momento.</div>
}

export default GoogleMaps
